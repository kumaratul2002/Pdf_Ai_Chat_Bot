{
  "name": "pdf-chat-bot",
  "version": "1.0.0",
  "description": "A PDF Chat Bot using React, Node.js, and Gemini AI",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["pdf", "chatbot", "react", "nodejs", "gemini", "langchain"],
  "author": "",
  "license": "MIT"
} 